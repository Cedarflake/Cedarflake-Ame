# Rust builder integration

This tracked directory contains the build scaffold that integrates Rust with Flutter. Keep its
source files in version control; only generated artifacts covered by the repository `.gitignore`
should be ignored.
