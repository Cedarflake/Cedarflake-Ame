# LiveOnly subtree recovery and browsing observations

Date: 2026-09-10

Status: C04 causal correction has focused and generated-client evidence; browsing and candidate
acceptance remain unresolved. The [roadmap](../roadmap.md) owns order and the
[execution plan](../plans/r2c-closeout.md) owns any further admission.

## C04 owning correction

The original +2000/-1500 experiment ended with 2012 published images and only 512 physical files.
Twelve LiveOnly subtree gaps exhausted eight P0 attempts because scoped promotion required a
journal opening boundary. Existing terminal rows had no eligible recovery consumer. Preserve that
failed catalog and its original 300-second result; it is distinct from C01's recovery timing issue.

The queue adapter now owns cohesive gap promotion, capacity and retained-debt modules. An explicit
current LiveOnly watcher subtree gap can atomically transfer to the same-scope P2 authority without
inventing journal continuity. Root generation, published namespace, absence proof, lineage and
publication safeguards remain required. Supported/unknown continuity does not take this exception.
The retained consumer revalidates eligible exhausted rows without resetting their attempts or
reviving unrelated failures. Capacity deferral preserves precise incoming work and lets existing
P2 work drain. Eligible retained debt remains unresolved work but does not deadlock that consumer
by counting as an unrelated exhausted blocker. No schema, dependency or bridge contract changes.

The metrics probe first checks for a relevant terminal candidate before compiling exact retained
qualification. This preserves the existing empty-queue VM bound; its assertion was not raised.
Independent review found scoped capacity/coalescing omissions and a same-root P2 capacity deadlock;
both received causal regressions and a scoped correction check.

Affected production line counts: queue facade 2101 (previously 2444), gap promotion 322, capacity
124, retained consumer 102, coalescing 1007, metrics 235 and persistence 1454; no inline tests in
those owners. Dedicated queue tests are 6146 plus promotion 558 and capacity 479. The new application
bulk workflow test is 338 lines and production retained-gap test 120. The existing production
runtime contains 4048 production and 10488 inline-test lines. Its decomposition remains roadmap
debt; this change adds only the dedicated test module declaration rather than new runtime behavior.

## Final-source verification

Starting commit: `7cad27e4b1ba4d60758f81a71101456b938ac468`, branch `codex/r2c`, with the C04
owned diff. No frontend product source changed. Final raw capture is
`build/diagnostics/r2c-p2-raw-6f890d3c145e464ba0e41a8bd17082fd`.

| Check | Result |
| --- | --- |
| Queue adapter regression group | 116 passed |
| Authoritative changes group | 15 passed |
| Production retained subtree and blocked-root exclusion | One passed each |
| Complete applicable lint | Passed, including formatting, guardrails, Clippy and Dart analysis |
| Owned verification process | Exit 0, process exited, Job closed; 293011 ms |
| Debug client build | Passed; owned build capture 62374 ms |

The actual application tests include over-page removal with an unchanged sibling, rollback,
ineligible continuity/namespace/state cases, full capacity, real P2 authority draining, final exact
publication and FULL catalog reopen. Preserve intermediate failures: the original causal failure,
the scoped leased-gap coalescing failure and the 512-VM-bound regression. Final passing evidence
follows owning corrections, not relaxed limits. This is 133 focused tests and lint, not a Daily pass.

Build capture: `build/diagnostics/r2c-p2-raw-8b1dc4a88ccf449f9e9a85efcd03aa93`.
SHA-256 identities:

- EXE: `DEE7E6CD4E0A621D310ED75FE7165C366556855ED473D357DE30A89EB6F510FA`.
- Rust DLL: `F800FD242C72E9214441F102D7827F75492FF50E67D5E3FC281B8E825F4914FF`.
- Debug kernel: `9B42FEDD8AA98BEB15D247F8FDEDDCC519DFED133EACB5037137C8621D747BBC`.

## Actual generated-source client evidence

Two lifetimes share the fixed 03:01:34–03:31:34 UTC deadline. They use the production application
entrypoint, real catalog/scanner/preview adapters and the existing Debug-only isolated storage
marker. All stimuli are generated sources. The original frozen 10000-file corpus spans twelve
dimensions and historical dates; its 72 repeated textured templates are not unique photographs.
The earlier catalog containing a manually added real root was not launched.

| Case | Exact oracle and timing |
| --- | --- |
| Retained original terminal debt, isolated database backup | 2012 to 512, background remains 10000; all work settled within 48316 ms of session start; no manual refresh |
| Fresh baseline and single changes | Import 12; add, rewrite, rename and remove match exact source/path/generation observations |
| Fresh +2000 | 2012 stimulus / 12012 total, exact paths and settled state within 26420 ms after copying |
| Fresh -1500 | 512 stimulus / 10512 total, exact publication at 31238 ms and settled work at 49433 ms after deletion |

Retained evidence pointer: `build/diagnostics/r2c-c04-retained-root.txt`, run
`1e148cc1ea0e4eb1b49b9fe8a68fc13b`. Fresh pointer:
`build/diagnostics/r2c-c04-fresh-root.txt`, run `7a5000a93aae4a08bb53cbb6f138bc03`.
The separate stimulus pointer is `build/diagnostics/r2c-c04-stimulus-root.txt`.
Its exclusive intent, source ledger and observation files preserve deliberate additions/removals.

Both application processes exited normally with closed Jobs. The first wrapper retained a
`finish-monitor: Owned monitor exit=` cleanup error because the helper exit code was unavailable;
the helper's own final receipt has no failure, and all owned PIDs subsequently retired. Retain
that wrapper error. Holding the helper process handle corrected capture for the second lifetime,
whose process/monitor receipts have no cleanup failure. Application lifetimes were 116625 and
1527369 ms; the second closed before the shared deadline, without a third launch.

| Resource evidence, bytes | Retained | Fresh |
| --- | ---: | ---: |
| Memory samples | 422 | 5754 |
| Peak working set | 507863040 | 1494200320 |
| Sampled peak private bytes | 471601152 | 1952202752 |
| Kernel peak commitment | 488329216 | 2037706752 |
| Minimum sampled system available | 5311119360 | 4527546368 |

The observed client/process peaks remain below 2 GiB and system samples above the 2 GiB reserve.
Fresh commitment approaches the ceiling; it is not evidence of a leak or ample headroom. The
preflight and process peak counters do not prove uninterrupted system-reserve sampling during the
short startup interval before the monitor binds the actual child. Keep that observation gap.

Full hash, file identity, byte count and historical-date checks pass for the unchanged 10000
sources. All 512 expected stimulus survivors match, totaling 704935470 bytes. The first final
source/catalog oracle ran while the client was alive; its evidence is preserved. A separate
`after-exit-catalog-oracle.json` then confirms normal retirement, the exact two roots and published
10000/512 counts, unchanged frozen paths/dimensions/dates, SQLite integrity and no foreign-key
violations. This SQLite check does not replace a native fresh-catalog FULL application reopen.
The retained source's independent 512-file integrity check also passes.

## C05 browsing findings — unresolved

These observations interrupt functional acceptance. They are grouped by workflow for investigation,
not assigned a shared cause. No frontend repair is claimed.

| Observation | Evidence / uncertainty | Required owning result |
| --- | --- | --- |
| C05-A: entire time rail repeatedly disappears | Reported during bulk publication; not frame-captured independently | Coherent timeline/geometry availability across an unchanged query |
| C05-B: direct timeline click leaves an empty photo wall until a wheel movement | Attached screenshot and independently observed persistent empty 2012-image middle viewport after synchronization settled | Current visible detail demand starts after direct navigation without extra input |
| C05-C: removal produces transient whole-wall thumbnail errors; later a bottom jump leaves gray cards without retry | User reports recovery of initial errors and persistent later gray cards; screenshot confirms placeholders, not decode or queue cause | Current visible source generation reaches pixels or truthful actionable failure; distinguish deleted old requests from surviving files |
| C05-D: entering/exiting preview briefly changes wall layout | User observation; offending frame not captured | Viewer return preserves the surviving asset anchor and stable geometry across frames |

The blank state was backed up before removal as `c04-timeline-blank-before-removal.sqlite3` with
observation metadata in the fresh client directory. Its 2012 source ledger is retained, while the
actual generated source now correctly has 512 files. A later screen has valid previews, which does
not establish recovery of every earlier blank viewport or dismiss the reported sequence.
Deletion of date groups can legitimately change the normalized rail position. Whether the visible
surviving asset also jumps is unverified; compare asset identity/local offset rather than assuming
the thumb must remain at the same percentage.

C05-B/C are S1 core browsing blockers. A/D remain severity-under-investigation symptoms attached
to that blocking workflow. Required scenarios are UX-03A/04A/05A/08A. Counts alone do not establish
their acceptance. The plan proposes bounded causal investigation and repair before any new run.

## Checkpoint and limits

Charge the full admitted 60 repair minutes and 15 active native-observation minutes conservatively,
including preparation, design delegation, failed attempts and result inspection. Charge five triage
minutes for the new browsing evidence and five closeout-recording minutes. Independent review and
its correction checks used 13 minutes plus one minute of final evidence reconciliation. Prior
849 minutes remain charged; the cycle checkpoint records the resulting 948/960 active minutes.
Owned test/build/client wall times above are separate from active work, and do not renew a quota.

C04 is implemented and focused/generated-client verified. Full Daily, C01/C02, remaining frozen
interleavings, Release/client acceptance, real downtime journal catch-up and installed-service
gates remain unresolved. Offline-add/reopen was not run in this C04 scope. C05 prevents usable
browsing acceptance. Neither this checkpoint nor a push accepts R2c or advances R3.
